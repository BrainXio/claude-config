{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-bootstrap",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-session-start",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-check-model-vision",
            "timeout": 60
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-pre-compact",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-session-end",
            "timeout": 30
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --from git+https://github.com/BrainXio/claude-cli.git claude-standards-guard",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
