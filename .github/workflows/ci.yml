name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  docs:
    uses: BrainXio/.github/workflows/doc-quality.yml@v1
    with:
      md-paths: "."
      yaml-paths: ".github"

  starter:
    uses: BrainXio/.github/workflows/starter-checks.yml@v1
